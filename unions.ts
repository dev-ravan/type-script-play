// Unions are used to declare a type that can have one of several possible types.It usefull when we want to allow 
// a variable or parameter to accept multiple types the syntex is |

const myId: string | number = "vel";