// Type inference refers to the compiler's 
// ability to automatically determine 
// the type of a variable, function, or expression without explicit type annotations.

let userName = "Ravan"; // string
let userID = 404; // number
let isUserAlive = true; //boolean

console.log(typeof userID,typeof userName,typeof isUserAlive);
